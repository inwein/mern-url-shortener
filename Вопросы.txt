Что такое Express? 

- - - - - - Экспресс - это фреймворк для node.js, который дает возможность собрать сервер

Что такое Mongoose и MongoDB? 

- - - - - - mongodb - это nosql база данных

Что такое Middleware? Концепт Middleware

- - - - - - middleware - это промежуточная функция, пример (req, res)

Что такое Endpoint ?

- - - - - - Endpoint - это url по которому мы запрашиваем данные по api 

REST Api

- - - - - - Rest api - это способ получения данных через оболочку в виде url (endpoint'a) 
При изменении параметров endpoint'a получаются разные данные.

6. Types в MongoDB и Types.ObjectID? 

7. express.json ? 

JSON.stringify() + JSON.parse() что дает ? 

- - - - - - Первый способ приводит к строке, второй приводит к объекту. 

9. Schema & model в moongose

- - - - - - Схема - это создание образца объекта данных, которые будут пулиться в БД, где указываются типы, которые должны заходить в этот объект, обязательны ли они, а также базовые проверки


// React 

7. Динамический параметр :id в react-router-dom Route

- - - - - - Указывается, чтобы можно было дальше привязывать динамически созданные урл в роутере

8. useCallback хук 

- - - - - - Оборачиваем в него коллбеки, чтобы у нас не было нескольких перевызовов и перерендеров. 

9. Зависимости в useEffect []

Зависимости - это те переменные, которые используются в хуке, и на изменение которых он может реагировать

10. Метод .save() в классах

11. Разобрать useHistory в react-router-dom

12. Разобрать useParams в react-router-dom

13. в fetch какой параметр какой по счету ?